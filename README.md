# React Tetris

This is a simple Tetris game built with React.

## Development

Install dependencies and start a dev server:

```bash
npm install
npm start
```

The game runs at `http://localhost:3000`.
